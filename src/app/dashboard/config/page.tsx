"use client"; import {useState} from "react"; import {supabase} from "@/lib/supabase"; export default function Page(){const [foto,setFoto]=useState(""); const upload=async(e:any)=>{const f=e.target.files[0]; if(!f)return; const url=URL.createObjectURL(f); setFoto(url); localStorage.setItem("ong_foto",url); const {data:{user}}=await supabase.auth.getUser(); if(user) await supabase.from("ongs").update({logo_url:url}).eq("usuario_id",user.id);}; return <div style={{padding:20}}><h3>Foto da ONG</h3><input type="file" onChange={upload} accept="image/*"/><br/>{foto&&<img src={foto} width={100} style={{borderRadius:50,marginTop:10}}/>}</div>}
